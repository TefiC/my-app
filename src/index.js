import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import 'react-bootstrap';
import 'bootstrap/dist/css/bootstrap.css';
import './index.css';
import $ from "jquery";
import { Modal, ModalBody, ModalDialog, ModalFooter, ModalHeader, ModalTitle, Button, FormControl } from 'react-bootstrap';

ReactDOM.render(
  <App />,
  document.getElementById('root')
);

